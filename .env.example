# Database
# SQLite (for development/testing)
DATABASE_URL="file:./dev.db"

# PostgreSQL (for production with connection pooling)
# DATABASE_URL="postgresql://user:password@localhost:5432/erp_db?schema=public&connection_limit=10&pool_timeout=20"

# NextAuth (if implementing authentication later)
NEXTAUTH_SECRET="your-secret-key-here"
NEXTAUTH_URL="http://localhost:3000"
